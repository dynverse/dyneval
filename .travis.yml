language: r
sudo: required
dist: trusty
bioc_required: true
cache: packages
addons:
  apt:
    packages:
      - libgsl-dev
      - libsdl1.2-dev
install:
  - R -e 'devtools::install_deps(upgrade = T, dependencies = T)'
script:
  - R CMD build .
  - R CMD check *tar.gz
